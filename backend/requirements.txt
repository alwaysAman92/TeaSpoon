# TeaSpoon backend dependencies (Python 3.9+)
fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy>=2.0.37
pydantic>=2.12.0
httpx==0.28.1
python-multipart==0.0.20
pyjwt[crypto]==2.10.1

# Dev / test
pytest==8.3.4

# Gemini API Integration
google-genai>=2.10.0

# NOTE: For production PostgreSQL, also install the driver from
# requirements-prod.txt (psycopg2-binary). SQLite is used by default in dev.
